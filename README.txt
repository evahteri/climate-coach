## How to run

1. In the root of the program folder, run ``` flutter run lib/main.dart ```

2. If the command line tool finds multiple devices, choose Chrome with number keys.

3. Close the program by closing the chrome window or by pressing Ctrl + C

